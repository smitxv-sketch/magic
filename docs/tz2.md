



# ПАТЧ 3 - отдельный  пункт меню IT GitHub рядом с шестеренкой
Это блестящий стратегический ход. На любом проектном комитете ИТ-директор и безопасник (ИБ) — это главные скептики. Если ты не дашь им четкий ответ на вопрос *"А сколько нам придется это кодить и насколько это дыряво?"*, они зарубят проект.

Отдельный экран «Для ИТ-специалистов» (или раздел «Документация / Интеграция») снимает эти возражения еще до того, как они прозвучат. Мы сразу показываем: мы говорим на их языке, мы не ломаем их архитектуру, и мы требуем от их отдела минимум ресурсов.

Вот готовое ТЗ на этот экран. Первая часть — это идеальные тезисы для слайда (Elevator Pitch), вторая — мясо для разработчиков 1С/Битрикс.

---

# ТЗ: Экран «Интеграция и ИТ-Онбординг» (IT Hub)

## 1. Концепция и UI/UX Логика

Новый раздел в главном меню Платформы: **«Для ИТ: Интеграция»** (Иконка `Terminal` или `Server`).

**Структура экрана:**

* **Шапка:** «Быстрый старт: Интеграция с вашей СЭД»
* **Верхний блок (Саммари):** Выжимка для ИТ-директора (4 главных тезиса).
* **Нижний блок (Аккордеон / Скролл):** Детальная техническая спецификация для программистов 1С / Bitrix24.

---

## 2. Контент: Верхний блок (Саммари для Проектного комитета)

> **Инструкция для верстки:** Этот блок оформляется в виде 4 крупных карточек в ряд (Grid). Это те самые аргументы, которые нужно вывести на слайд презентации.

* ⚡ **Time-to-market: 20-30 часов**
Для внедрения не нужно менять ядро 1С:ДО. Платформа работает как внешний микросервис. Интеграция требует настройки одного исходящего Webhook и одного парсера входящего JSON силами штатного программиста 1С.
* 🔒 **Stateless Архитектура (Zero Retention)**
Платформа не хранит корпоративные документы. Текст передается в LLM транзитом (в оперативной памяти), анализируется и немедленно удаляется. Мы возвращаем только результат проверки (JSON).
* 🔌 **Универсальный REST API**
Связь между вашей СЭД и Платформой осуществляется по стандартному HTTP/HTTPS протоколу. Поддерживается базовая аутентификация по Bearer-токену.
* 🛡️ **Готовность к On-Premise (Контур предприятия)**
Для демо-стенда используется облачная модель (SaaS). Для боевого контура Платформа и выбранная нейросеть (например, локальная Llama 3 или GigaChat) могут быть развернуты полностью внутри DMZ предприятия без доступа в интернет.

---

## 3. Контент: Нижний блок (Детальный план для Разработчика)

> **Инструкция для верстки:** Этот блок оформляется в виде пошагового руководства (Step-by-step guide) с примерами кода.

### Шаг 1: Настройка триггера в 1С / Bitrix24 (Webhook)

На нужном этапе бизнес-процесса (например, статус «На проверке») ваш разработчик добавляет скрипт, который формирует POST-запрос на наш Endpoint: `https://api.your-platform.ru/v1/inference`.

**Что нужно передать (Payload):**

```json
{
  "document_id": "1C-DOC-8472",
  "cube_id": "node_legal_check_01",
  "document_text": "Сырой текст документа извлекается силами 1С...",
  "placeholders": {
    "Сумма_договора": "1500000",
    "ИНН_Контрагента": "7453198234"
  }
}

```

### Шаг 2: Обработка ответа от AI-Платформы

В течение 10–20 секунд Платформа вернет в вашу СЭД синхронный ответ строго в формате JSON. Ответ всегда валидируется по схеме, поэтому 1С никогда не получит «грязный» текст, из-за которого упадет парсер.

**Пример ответа Платформы:**

```json
{
  "status": "success",
  "analysis": {
    "severity_score": 8,
    "has_blocking_issue": true,
    "findings": [
      { "type": "Юридический", "text": "Пени превышают 0.1%" }
    ]
  },
  "execution_command": {
    "action_id": "return_to_author",
    "comment_to_user": "AI: Найдены кабальные условия по штрафам.",
    "target_field_name": null
  }
}

```

### Шаг 3: Маппинг действий (Action Execution)

Вашему разработчику нужно написать простой `switch-case` обработчик в 1С, который реагирует на поле `action_id`.

**Платформа поддерживает 7 стандартизированных команд:**

1. `continue_process` ➔ 1С переводит документ на следующий этап согласования.
2. `return_to_author` ➔ 1С меняет статус на "Доработка" и записывает `comment_to_user` в историю.
3. `escalate` ➔ 1С меняет исполнителя задачи на руководителя.
4. `add_comment` ➔ 1С просто добавляет текст в таймлайн без смены статуса.
5. `start_subprocess` ➔ 1С запускает фоновый БП по переданному `target_process_id`.
6. `set_field` ➔ 1С записывает значение в доп. реквизит документа.
7. `skip_node` ➔ Игнорировать результат AI (в случае таймаута).

### Шаг 4: Сеть и Безопасность

* **Авторизация:** Сгенерируйте API Token в настройках Платформы и передавайте его в заголовке `Authorization: Bearer <token>`.
* **IP Whitelisting:** Для боевого контура мы предоставляем статические IP-адреса Платформы для добавления в белые списки ваших межсетевых экранов (Firewall).
* **Отказоустойчивость:** Рекомендуется настроить таймаут ожидания на стороне 1С в размере **30 секунд**. Если Платформа не ответила, СЭД должна перевести документ по ветке `skip_node` (пропуск на ручной контроль), чтобы не блокировать работу предприятия.

---

## 4. Визуальные требования к экрану

1. **Код-блоки:** Все примеры JSON должны использовать библиотеку синтаксической подсветки (например, `react-syntax-highlighter` или стандартный Tailwind Typography с `prose-pre:bg-surface-elevated`). Добавить кнопку "Copy to clipboard" в правом верхнем углу каждого блока кода.
2. **Скачиваемый артефакт:** В самом низу страницы добавить кнопку **«Скачать полную спецификацию API (Swagger/OpenAPI .yaml)»**. *(Для демо-стенда это может быть просто кнопка-заглушка или ссылка на красивый PDF-файл, но она обязана там быть для солидности)*.

---





# Патч 4 -  Плеер и  анимация

Нам нужен вариант для энтерпрайза: четкие карточки, цветовое кодирование шагов , мягкие тени и ощущение структурированности.

Текущий вариант «Плеера» (второй скриншот) выглядит как сырой прототип. Иконки висят в воздухе, нет ощущения «конвейера», и глазу не за что зацепиться. Понятно, почему анимация не работает — сейчас там нет физического "трека", по которому документ мог бы ехать.

Давай превратим твой текущий экран в премиальный продукт и заставим Framer Motion работать как часы.

---

### Часть 1. Визуальный апгрейд (Уроки из твоего референса)

Чтобы сделать как на первом скрине, нам нужно уйти от «голых иконок» к **Карточкам Узлов (Node Cards)**.

1. **Единая линия (Track):** За узлами должна проходить сплошная горизонтальная линия (светло-серая). Это наш конвейер. Она показывает маршрут.
2. **Карточки вместо иконок:** Каждый шаг — это белая карточка с рамкой (`bg-white rounded-xl border p-4 shadow-sm`).
3. **Цветовое кодирование (как на твоем референсе):**
* *Стандартный узел (человек):* Серая или синяя рамка.
* *AI-узел:* Фиолетовая или градиентная рамка, чтобы выделялся интеллект.
* *Активный узел:* Сильное свечение (Glow) и пульсация.
* *Пройденный узел:* Зеленая галочка и тусклый цвет (opacity: 0.6).
Но помни что у нас дизайн система на токенах !!!

4. **Типографика:** Заголовок шага должен быть жирным (`font-semibold`), а описание (subtitle) — мелким и серым (`text-xs text-gray-500`).

---

### Часть 2. Почему не работает анимация и как её починить

В React (особенно с Vite) анимация таких конвейеров обычно ломается из-за того, что движущийся маркер не знает, **куда именно (на какие координаты X)** ему ехать.

**Золотой стандарт анимации БП на Framer Motion:**
Мы не анимируем сами карточки. Мы создаем **один** плавающий элемент (Маркер документа) и с помощью `motion.div` двигаем его по оси X.

Вот пошаговый рецепт и архитектура кода, как это реализовать прямо сейчас.

#### Шаг 1: Правильная структура сетки (CSS)

Мы используем `flex` или `grid`, чтобы выстроить карточки. А линию трека и маркер делаем `absolute`.

#### Шаг 2: Реализация компонента ProcessMap.tsx (Готовый шаблон)

Передай этот код разработчику или AI-ассистенту. Это готовый паттерн для Framer Motion, который решает проблему вычисления координат.

```tsx
import React, { useEffect, useRef, useState } from 'react';
import { motion } from 'framer-motion';
// Предположим, у нас есть Zustand store с текущим индексом
// import { useAppStore } from '../store/appStore';

const nodes = [
  { id: 'init', title: 'Инициатор', type: 'human', subtitle: 'Создание' },
  { id: 'ai1', title: 'AI: Нормоконтроль', type: 'ai', subtitle: 'Орфография' },
  { id: 'ai2', title: 'AI: Пре-юрист', type: 'ai', subtitle: 'Риски' },
  { id: 'legal', title: 'Юрист', type: 'human', subtitle: 'Проверка' },
];

export const ProcessMap = () => {
  // activeIndex берется из Zustand (0, 1, 2, 3...)
  const [activeIndex, setActiveIndex] = useState(0); 
  
  // Массив рефов для каждой карточки, чтобы знать их координаты X
  const nodeRefs = useRef<(HTMLDivElement | null)[]>([]);
  const [markerPosition, setMarkerPosition] = useState(0);

  // Как только меняется активный шаг, вычисляем новую позицию маркера
  useEffect(() => {
    const activeElement = nodeRefs.current[activeIndex];
    if (activeElement) {
      // Вычисляем центр активной карточки относительно родителя
      const offsetLeft = activeElement.offsetLeft;
      const width = activeElement.offsetWidth;
      setMarkerPosition(offsetLeft + width / 2 - 20); // 20 - половина ширины маркера
    }
  }, [activeIndex]);

  return (
    <div className="relative w-full max-w-6xl mx-auto py-20 px-10">
      
      {/* 1. Серая линия конвейера (Трек) на заднем фоне */}
      <div className="absolute top-1/2 left-10 right-10 h-1 bg-gray-200 -translate-y-1/2 z-0" />

      {/* 2. Заполненная линия (Прогресс-бар) */}
      <motion.div 
        className="absolute top-1/2 left-10 h-1 bg-primary -translate-y-1/2 z-0"
        animate={{ width: markerPosition }} // Линия тянется за маркером
        transition={{ type: "spring", stiffness: 60, damping: 15 }}
      />

      {/* 3. Анимированный Маркер документа */}
      <motion.div
        className="absolute top-1/2 w-10 h-10 bg-white border-4 border-primary rounded-full shadow-lg z-20 flex items-center justify-center"
        initial={false}
        animate={{ 
          x: markerPosition,
          y: '-50%' 
        }}
        transition={{ type: "spring", stiffness: 60, damping: 15 }}
      >
        📄
      </motion.div>

      {/* 4. Сами Карточки узлов */}
      <div className="relative flex justify-between items-center z-10">
        {nodes.map((node, index) => {
          const isActive = index === activeIndex;
          const isPassed = index < activeIndex;

          return (
            <div 
              key={node.id} 
              ref={(el) => (nodeRefs.current[index] = el)}
              className="flex flex-col items-center"
            >
              <div className={`
                w-48 bg-white border-2 rounded-xl p-4 shadow-sm transition-all duration-300
                ${isActive ? 'border-primary shadow-glass scale-105 ring-4 ring-primary-light' : ''}
                ${isPassed ? 'border-status-success-border bg-gray-50' : ''}
                ${!isActive && !isPassed ? 'border-gray-200' : ''}
              `}>
                <div className="flex items-center gap-2 mb-2">
                   <span className="text-xl">
                     {node.type === 'ai' ? '🤖' : '👤'}
                   </span>
                   <span className={`text-sm font-bold ${isActive ? 'text-primary' : 'text-gray-700'}`}>
                     {node.title}
                   </span>
                </div>
                <p className="text-xs text-gray-500">{node.subtitle}</p>
              </div>
            </div>
          );
        })}
      </div>
      
      {/* Кнопка для теста анимации (в реальности этим управляет usePlayerEngine) */}
      <div className="mt-10 flex justify-center">
        <button 
          onClick={() => setActiveIndex((prev) => Math.min(prev + 1, nodes.length - 1))}
          className="px-6 py-2 bg-primary text-white rounded-lg shadow-md hover:bg-primary-dark"
        >
          Симуляция: Следующий шаг ➔
        </button>
      </div>

    </div>
  );
};

```

### Разбор магии (Как это работает):

1. **`nodeRefs`:** Мы привязываем ссылку (`ref`) к каждой карточке.
2. **Вычисление X:** Когда статус меняется, React смотрит: "Ага, активен шаг №2. Где он находится на экране?". Берет его координаты (`offsetLeft`) и говорит Маркеру: "Едь туда".
3. **Framer Motion (`motion.div`):** Вместо того чтобы телепортировать маркер, свойство `transition={{ type: "spring" }}` заставляет его плавно и красиво переехать на новую точку с эффектом легкой пружины.

### Что передать ИИ или разработчику:

1. Скажи: *"Замени текущий макет Плеера на сетку с карточками (Card-based UI). Добавь рамки, тени и скругления как в современных дашбордах."*
2. Скажи: *"Анимацию документа реализуй через `framer-motion` с использованием расчета позиций карточек через `useRef` и `offsetLeft`, чтобы маркер физически ехал над линией прогресса."*