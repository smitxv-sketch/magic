import { 
  FileText, Shield, Zap, GitMerge, Server, Lock, Bot, HardHat, 
  FileStack, Users, AlertTriangle, ShieldCheck, Scale, Mail, CheckCircle
} from 'lucide-react';

export const ICON_MAP: Record<string, React.ElementType> = {
  FileText, Shield, Zap, GitMerge, Server, Lock, Bot, HardHat, 
  FileStack, Users, AlertTriangle, ShieldCheck, Scale, Mail, CheckCircle
};
